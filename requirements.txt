# 核心依赖
playwright==1.57.0
pytest
pytest-playwright
allure-pytest
python-dotenv

# 测试增强
pytest-xdist           # 并行测试执行
pytest-rerunfailures   # 失败重试机制
pytest-timeout        # 测试超时控制

allure-python-commons