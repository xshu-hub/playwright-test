# ==============================================================================
# Playwright 自动化测试框架 - 环境变量配置
# ==============================================================================
# 使用方法：
# 1. 复制此文件为 .env：cp .env.example .env
# 2. 根据你的测试系统修改「目标系统配置」部分
# 3. 框架会自动加载 .env 文件中的配置
# ==============================================================================

# ==============================================================================
# 目标系统配置（必须根据你的测试系统修改）
# ==============================================================================

# 测试目标网站 URL
# 修改为你的测试系统地址
BASE_URL=https://your-test-system.example.com

# 测试用户凭证
# 修改为你的测试系统的管理员账号
ADMIN_USER=your_admin_username
ADMIN_PASSWORD=your_admin_password

# Session 验证路径
# 用于验证 session 是否有效的页面路径（需要登录才能访问）
# 例如：/dashboard, /home, /admin
SESSION_VALIDATION_PATH=/dashboard

# 登录页面 URL 特征
# 用于检测是否被重定向到登录页
# 例如：/login, /signin, /auth
LOGIN_URL_PATTERN=/login

# ==============================================================================
# OrangeHRM 示例配置（如果测试 OrangeHRM Demo，取消以下注释）
# ==============================================================================
# BASE_URL=https://opensource-demo.orangehrmlive.com
# ADMIN_USER=Admin
# ADMIN_PASSWORD=admin123
# SESSION_VALIDATION_PATH=/dashboard
# LOGIN_URL_PATTERN=/login

# ==============================================================================
# 框架通用配置（通常无需修改）
# ==============================================================================

# 默认超时时间（毫秒）
TIMEOUT=30000

# 无头模式
# true: 后台运行（适用于 CI/CD）
# false: 显示浏览器窗口（适用于调试）
HEADLESS=true

# 慢动作模式延迟（毫秒）
# 用于调试，0 表示不延迟
SLOW_MO=0

# 浏览器视口大小
VIEWPORT_WIDTH=1920
VIEWPORT_HEIGHT=1080

# 测试失败时自动截图
SCREENSHOT_ON_FAILURE=true

# ==============================================================================
# Session 复用配置
# ==============================================================================

# 是否复用已保存的登录 Session
# 启用后可跳过登录流程，显著加速测试
# 也可通过命令行参数 --reuse-session 启用
REUSE_SESSION=false

# Session 文件名（保存在 data/sessions/ 目录下）
SESSION_FILE=auth_state.json

# ==============================================================================
# 日志配置
# ==============================================================================

# 控制台日志级别：DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# 文件日志级别
FILE_LOG_LEVEL=DEBUG

# 日志文件大小限制（字节），默认 5MB
LOG_MAX_BYTES=5242880

# 日志文件备份数量
LOG_BACKUP_COUNT=5
